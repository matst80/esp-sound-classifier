# Core dependencies (cross-platform)
librosa>=0.9.0
numpy>=1.21.0
soundfile>=0.12.0

# Coral/TFLite - Linux only, install separately:
# pip install --extra-index-url https://google-coral.github.io/py-repo/ pycoral~=0.2 tflite-runtime
